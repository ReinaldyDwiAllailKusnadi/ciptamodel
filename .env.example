# --- CiptaModel core ---
PORT=3000
BASE_URL=http://localhost:3000
DATABASE_PATH=./data/ciptamodel.db
SESSION_SECRET=change-me-to-a-long-random-string-min-32-chars

# --- Optional Redis for distributed rate limiting (abstraction ready; in-memory used when empty) ---
REDIS_URL=

# --- Public contract ---
PUBLIC_API_BASE_URL=https://ciptamodel.com/v1

# NOTE (Phase 1): no provider credentials exist yet. Provider adapters are
# registry/interface only; upstream wiring lands in Phase 2.
