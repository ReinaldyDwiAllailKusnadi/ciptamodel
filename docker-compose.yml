services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file: .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
  # Optional Redis for distributed rate limiting (Phase 4):
  # redis:
  #   image: redis:7-alpine
  #   ports: ["6379:6379"]
